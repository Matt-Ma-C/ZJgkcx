# 测试报告

测试日期：2026-07-27

## 自动化测试

最终结果：18 项全部通过。

1. 岗位数据覆盖 2024—2026 年、浙江省级机关及 11 个设区市。
2. 14,200 个岗位 ID 无重复。
3. 市—区县层级完整，岗位行政区归属有效，并包含“金华市—金东区”等实际岗位。
4. 缴费人数与报录比计算一致。
5. 2025、2026 年均有超过 3,000 个岗位，缴费人数匹配率均高于 98%。
6. 各岗位“最低进面分”与进面人员最低笔试成绩一致。
7. 同一岗位内进面和录用人员准考证号无重复。
8. 所有有限条件的筛选选项均由岗位数据生成。
9. 本地首页可正常构建并渲染。
10. 设区市为单选，区县为联动多选，点击控件外部会自动关闭。
11. 蓝白主题变量和界面样式已生效。
12. Markdown 导出包含指定考情字段，以及笔试、面试成绩的最低值、P25、中位数、均值、P75 和最高值。
13. DeepSeek 分析在岗位为空、API Key 为空或岗位超过 1,000 个时不会发起外部调用。
14. DeepSeek 请求使用当前模型、固定考情分析系统提示词和 Bearer 鉴权，项目中未写入任何 API Key。
15. 单个岗位支持隐藏和取消隐藏，隐藏项保持灰色展示，并从导出、分析数据中排除。
16. 公安岗位支持纳入或排除分析；排除时数据和系统提示词同步限制，纳入时要求验证“加试专业岗位可能形成竞争洼地”的假设。
17. DeepSeek 分析正文通过 GFM Markdown 渲染器显示，表格、标题、列表、引用和代码块样式完整。
18. 系统提示词强制输出筛选条件影响因子表，逐项覆盖全部筛选维度，并单独比较政治面貌要求与不限岗位。

## 数据验证

- 招录岗位：14,200 个。
- 年度分布：2024 年 5,022 个、2025 年 5,174 个、2026 年 4,004 个。
- 已关联缴费人数岗位：14,106 个。
- 2024 年缴费匹配：4,970 / 5,022。
- 2025 年缴费匹配：5,146 / 5,174。
- 2026 年缴费匹配：3,990 / 4,004。
- 进面/总成绩人员记录：43,276 条，覆盖 10,316 个岗位。
- 最终录用人员记录：16,342 条，覆盖 11,585 个岗位。
- 设区市选项：省级机关及 11 个设区市，共 12 个一级选项。
- “金华市—金东区”岗位：54 个。

## 构建与运行验证

- `vinext build` 构建成功。
- ESLint 检查通过，无错误或警告。
- 本地首页返回 HTTP 200。
- 本地数据接口返回 HTTP 200，岗位数、市—区县层级及人员记录数与生成数据一致。
- 本地 DeepSeek 代理接口已验证空岗位返回 HTTP 400，且不会访问外部服务。
- 使用临时测试密钥完成一次端到端调用：本地 `/api/analyze` 返回 HTTP 200，模型为 `deepseek-v4-flash`，分析正文正常返回。
- DeepSeek API Key 不写入项目文件或浏览器存储；人员姓名和准考证号不进入分析请求。
- 开发服务器当前运行于 <http://localhost:3000/>。

## 原始附件说明

- 本次构建读取 2,469 份原始文件，并支持 Excel、WPS 旧表格、Word 表格、压缩包及官网内嵌 HTML 表格。
- 对扩展名错误的附件按内部文件结构识别，原文件未被修改。
- 官网已删除、损坏、旧式加密或没有有效表格内容的附件按实际可用情况跳过；相关岗位仍保留岗位和缴费等已成功关联的数据。
- 人员名单不做猜测性关联，无法唯一定位岗位的记录留在构建报告中，不显示在错误的岗位下。
